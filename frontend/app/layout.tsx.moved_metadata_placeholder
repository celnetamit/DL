import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Digital Library | Explore our Catalog",
  description: "Advanced digital library and learning platform. Explore journals, books, and articles in specialized domains.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
